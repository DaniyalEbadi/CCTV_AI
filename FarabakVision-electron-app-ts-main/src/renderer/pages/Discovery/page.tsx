import ModeSelector from "./ui/ModeSelector";

const DiscoveryPage = () => {
  return (
    <div className="w-full overflow-y-auto flex flex-col h-full no-scrollbar">
      <ModeSelector />
    </div>
  );
};

export default DiscoveryPage;
